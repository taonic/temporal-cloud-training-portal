apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Container-to-container, so the service name rather than localhost.
    url: http://prometheus:9090
    isDefault: true
    editable: true
