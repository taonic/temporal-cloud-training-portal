node_modules/
.next/
# NEXT_DIST_DIR target, for building while `pnpm dev` holds .next
.next-build/
out/
dist/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env*.local

# mTLS material — never commit
*.pem
*.key
*.crt

# uv's virtualenv for labs/worker. uv.lock IS committed — a workshop wants every
# attendee resolving the same versions, and it is what makes `uv run` offline-safe
# once the preset has warmed the cache.
.venv/
__pycache__/
*.pyc

.DS_Store

# labs/worker local config
labs/worker/.env

course.md
quiz.md
nice-to-have.md

# Instructor-only Rate Desk credential (labs/worker/.env.desk.example is the template)
labs/worker/.env.desk
